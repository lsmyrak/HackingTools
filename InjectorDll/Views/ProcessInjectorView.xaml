<UserControl x:Class="InjectorDll.Views.ProcessInjectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:InjectorDll.Views"
             xmlns:components="clr-namespace:InjectorDll.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid
      Style="{StaticResource MainWindow}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <components:ProcesListView Grid.Column="0" Grid.Row="1" DataContext="{Binding ProcessListViewModel}"/>
        <components:DllListView Grid.Column="1" Grid.Row="1"  DataContext="{Binding DllListViewModel}"/>

    </Grid>
</UserControl>
