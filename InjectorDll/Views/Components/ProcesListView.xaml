<UserControl x:Class="InjectorDll.Views.Components.ProcesListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InjectorDll.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800">
    <Grid 
        Margin="2"       
        >

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid
            Grid.Row="0"
            Margin="2"            
           >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
                <Label                                     
                    Grid.Column="0"
                    FontSize="25"                    
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Content="Process List:"            
                />
                
        </Grid>
        <WrapPanel
            Grid.Row="1">
            <Label 
                Content="Find the process:"
                FontSize="15"
                />
            <TextBox 
                Text="{Binding Finder , Mode=TwoWay ,UpdateSourceTrigger=PropertyChanged}"
                MinWidth="200"
                />
            <Button 
                Content="Clear Filtr"
                Command="{Binding CmdClearFinder}"
                />
            <Button
                Grid.Column="1"
       Command="{Binding CmdRefreshProcessList}"
       Content="Refresh process list"                
    />
        </WrapPanel>
        <DataGrid 
            Grid.Row="2"
            ItemsSource="{Binding ProcessIdNameViewModels, Mode=TwoWay}"
            SelectedItem="{Binding SelectedProcess}"
            SelectionUnit="FullRow"
            SelectionMode="Single"
            MaxHeight="250"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            ScrollViewer.CanContentScroll="True" 
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            ScrollViewer.HorizontalScrollBarVisibility="Auto"
                   >
            <DataGrid.Columns>
                <DataGridTextColumn
                    Header="Pid :"
                    Binding="{Binding Pid}"
                    MinWidth="200"
                    />
                <DataGridTextColumn
                    Header="Process Name:"
                    Binding="{Binding Name}"
                    MinWidth="50"
                    />
                    <DataGridTextColumn
                    Header="Windndow Title:"
                    Binding="{Binding WindowTitle}"
                     />
            </DataGrid.Columns>
        </DataGrid>
        <WrapPanel 
            Grid.Row="3" 
            VerticalAlignment="Bottom" 
            HorizontalAlignment="Center"
            >
            <Button 
                Content="Inject dll to process" 
                />
            </WrapPanel>
    </Grid>
</UserControl>
